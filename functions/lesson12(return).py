#return is used to return a value from a function. It can be used to return any type of value, including numbers, strings, lists, and even other functions.
def add(a,b): # this is used to define a function called add that takes two arguments called a and b, the arguments are variables that are used to pass information
    return a+b # different from print function, return is used to return a value from a function, so that we can use that value later in the code, while print function is used to print a value to the console, so that we can see the output of the code when we run it, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function
result= add(5,3) # this is used to call the function add and pass 5 and 3 as arguments to the function, and store the returned value in the result variable, so that we can use that value later in the code, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function
#this 5,3 goes to top add(a,b) and thne assign to return and get the output and store it in result variable, so that we can use that value later in the code, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function
print(result)  #here we only print the result variable which is used to store the returned value from the add function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function
 #the return used to pass the value from the function to the variable result, so that we can use that value later in the code, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function 
 