def add(a, b):
    return a + b

#if we use print instead of return in the add function then it will print the sum of a and b when the function is called, but it will not return the value to the caller, so we cannot use that value later in the code, so the output will be like this 8 because we use print function to print the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use print function to print the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, but if we use return statement instead of print function in the add function then it will return the sum of a and b to the caller, so that we can use that value later in the code, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function, so the output will be like this 8 because we use return statement to return the sum of a and b when the function is called and we pass 5 and 3 as arguments to the function
#yes